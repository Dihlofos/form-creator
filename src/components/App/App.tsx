import { ContentBox } from 'ui';
import React, { FC } from 'react';
import FormTabs from '../FormTabs/FormTabs';

const App: FC = () => (
  <ContentBox>
    <FormTabs />
  </ContentBox>
);

export default App;
